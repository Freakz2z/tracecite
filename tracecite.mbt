/// TraceCite validates that an Agent answer's source references can be
/// traced to source records returned by completed tool calls.
/// The JSONL event contract and validator are the next implementation step.
